select:
  - platform: logger
    id: logger_select
    name: Logger Level

  - platform: template
    name: Effects
    entity_category: config
    id: effects
    optimistic: true
    options:
      - None
      - Rainbow
      - Slow Pulse
      - Color Wipe
      - Wipe In
      - Wipe Out
      - Flames Vertical Right
      - Flames Vertical Left
      - Police All
      - Cycle Colors
      - Wheel of Color
      - Fire
      - Blue fire
      - Lightning
      - Falling light
      - Rise light
      - Simple light
      - HSV
      - Christmas RedGreen
      - Pulse
      - Fast Pulse
      - Scan
      - Twinkle
      - Random Twinkle
      - Fireworks
      - Fireworks Effect With Custom Values
      - Flicker
      - Strobe
      - Chroma Haze
      - ColorScan
      - Candy Cane
      - Vesuvius
      - Blue Scan
      - Fireworks 2024
      - Wave
      - Wave Fade
      - Wave Comp
      - Wave Comp Fade
      - Wave Rotate
      - Wave Rotate Fade
      - Christmas Tree - Static
      - Christmas Tree - Fading
      - Christmas Tree - Multicolour
    initial_option: None
    restore_value: true
    on_value:
      then:
        # LIVE SET VALUES INTO LIGHTS IF THEY ARE IN "ON" STATE
        - if:
            condition:
              - lambda: 'return id(light_step_00_state) == "on";'
            then:
              - light.control:
                  id: step_00
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_01_state) == "on";'
            then:
              - light.control:
                  id: step_01
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_02_state) == "on";'
            then:
              - light.control:
                  id: step_02
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_03_state) == "on";'
            then:
              - light.control:
                  id: step_03
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_04_state) == "on";'
            then:
              - light.control:
                  id: step_04
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_05_state) == "on";'
            then:
              - light.control:
                  id: step_05
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_06_state) == "on";'
            then:
              - light.control:
                  id: step_06
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_07_state) == "on";'
            then:
              - light.control:
                  id: step_07
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_08_state) == "on";'
            then:
              - light.control:
                  id: step_08
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_09_state) == "on";'
            then:
              - light.control:
                  id: step_09
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_10_state) == "on";'
            then:
              - light.control:
                  id: step_10
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_11_state) == "on";'
            then:
              - light.control:
                  id: step_11
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_12_state) == "on";'
            then:
              - light.control:
                  id: step_12
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_13_state) == "on";'
            then:
              - light.control:
                  id: step_13
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_14_state) == "on";'
            then:
              - light.control:
                  id: step_14
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_15_state) == "on";'
            then:
              - light.control:
                  id: step_15
                  effect: !lambda "return (id(effects).current_option());"
        - if:
            condition:
              - lambda: 'return id(light_step_16_state) == "on";'
            then:
              - light.control:
                  id: step_16
                  effect: !lambda "return (id(effects).current_option());"

  - platform: template
    name: Color
    entity_category: config
    id: color
    optimistic: true
    restore_value: true
    options:
      - white
      - red
      - green
      - blue
      - cyan
      - orange
      - yellow
      - pink
      - magenta
      - lime
      - teal
      - amber
      - navy
      - maroon
      - olive
      - brown
      - gold
      - silver
      - aqua
      - lavender
      - "2000k"
      - "3000k"
      - "4000k"
      - "5000k"
      - "6000k"

    initial_option: white
    on_value:
      then:
        - if:
            condition:
              lambda: 'return id(color).current_option() == "white";'
            then:
              - globals.set:
                  id: color_red
                  value: "1.0"
              - globals.set:
                  id: color_green
                  value: "1.0"
              - globals.set:
                  id: color_blue
                  value: "1.0"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "red";'
            then:
              - globals.set:
                  id: color_red
                  value: "1.0"
              - globals.set:
                  id: color_green
                  value: "0.0"
              - globals.set:
                  id: color_blue
                  value: "0.0"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "blue";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.0"
              - globals.set:
                  id: color_green
                  value: "0.0"
              - globals.set:
                  id: color_blue
                  value: "1.0"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "cyan";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.0"
              - globals.set:
                  id: color_green
                  value: "1.0"
              - globals.set:
                  id: color_blue
                  value: "1.0"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "green";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.0"
              - globals.set:
                  id: color_green
                  value: "1.0"
              - globals.set:
                  id: color_blue
                  value: "0.0"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "orange";'
            then:
              - globals.set:
                  id: color_red
                  value: "1.0"
              - globals.set:
                  id: color_green
                  value: "0.65"
              - globals.set:
                  id: color_blue
                  value: "0.0"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "yellow";'
            then:
              - globals.set:
                  id: color_red
                  value: "1.0"
              - globals.set:
                  id: color_green
                  value: "1.0"
              - globals.set:
                  id: color_blue
                  value: "0.0"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "pink";'
            then:
              - globals.set:
                  id: color_red
                  value: "1.0"
              - globals.set:
                  id: color_green
                  value: "0.41"
              - globals.set:
                  id: color_blue
                  value: "0.71"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "magenta";'
            then:
              - globals.set:
                  id: color_red
                  value: "1.0"
              - globals.set:
                  id: color_green
                  value: "0.0"
              - globals.set:
                  id: color_blue
                  value: "1.0"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "lime";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.0"
              - globals.set:
                  id: color_green
                  value: "1.0"
              - globals.set:
                  id: color_blue
                  value: "0.2"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "teal";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.0"
              - globals.set:
                  id: color_green
                  value: "0.5"
              - globals.set:
                  id: color_blue
                  value: "0.5"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "amber";'
            then:
              - globals.set:
                  id: color_red
                  value: "1.0"
              - globals.set:
                  id: color_green
                  value: "0.75"
              - globals.set:
                  id: color_blue
                  value: "0.0"
        - if:
            condition:
                lambda: 'return id(color).current_option() == "navy";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.0"
              - globals.set:
                  id: color_green
                  value: "0.0"
              - globals.set:
                  id: color_blue
                  value: "0.5"
        - if:
            condition:
                lambda: 'return id(color).current_option() == "maroon";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.5"
              - globals.set:
                  id: color_green
                  value: "0.0"
              - globals.set:
                  id: color_blue
                  value: "0.0"
        - if:
            condition:
                lambda: 'return id(color).current_option() == "olive";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.5"
              - globals.set:
                  id: color_green
                  value: "0.5"
              - globals.set:
                  id: color_blue
                  value: "0.0"
        - if:
            condition:
                lambda: 'return id(color).current_option() == "brown";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.65"
              - globals.set:
                  id: color_green
                  value: "0.16"
              - globals.set:
                  id: color_blue
                  value: "0.16"
        - if:
            condition:
                lambda: 'return id(color).current_option() == "gold";'
            then:
              - globals.set:
                  id: color_red
                  value: "1.0"
              - globals.set:
                  id: color_green
                  value: "0.84"
              - globals.set:
                  id: color_blue
                  value: "0.0"
        - if:
            condition:
                lambda: 'return id(color).current_option() == "silver";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.75"
              - globals.set:
                  id: color_green
                  value: "0.75"
              - globals.set:
                  id: color_blue
                  value: "0.75"
        - if:
            condition:
                lambda: 'return id(color).current_option() == "aqua";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.0"
              - globals.set:
                  id: color_green
                  value: "1.0"
              - globals.set:
                  id: color_blue
                  value: "1.0"
        - if:
            condition:
                lambda: 'return id(color).current_option() == "lavender";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.9"
              - globals.set:
                  id: color_green
                  value: "0.9"
              - globals.set:
                  id: color_blue
                  value: "0.98"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "2000k";'
            then:
              - globals.set:
                  id: color_red
                  value: "1.0"
              - globals.set:
                  id: color_green
                  value: "0.55"
              - globals.set:
                  id: color_blue
                  value: "0.2"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "3000k";'
            then:
              - globals.set:
                  id: color_red
                  value: "1.0"
              - globals.set:
                  id: color_green
                  value: "0.7"
              - globals.set:
                  id: color_blue
                  value: "0.4"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "4000k";'
            then:
              - globals.set:
                  id: color_red
                  value: "1.0"
              - globals.set:
                  id: color_green
                  value: "0.85"
              - globals.set:
                  id: color_blue
                  value: "0.7"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "5000k";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.9"
              - globals.set:
                  id: color_green
                  value: "0.9"
              - globals.set:
                  id: color_blue
                  value: "1.0"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "6000k";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.8"
              - globals.set:
                  id: color_green
                  value: "0.85"
              - globals.set:
                  id: color_blue
                  value: "1.0"
        - if:
            condition:
              lambda: 'return id(color).current_option() == "7000k";'
            then:
              - globals.set:
                  id: color_red
                  value: "0.72"
              - globals.set:
                  id: color_green
                  value: "0.80"
              - globals.set:
                  id: color_blue
                  value: "1.0"

        # LIVE SET VALUES INTO LIGHTS IF THEY ARE IN "ON" STATE
        - if:
            condition:
              - lambda: 'return id(light_step_00_state) == "on";'
            then:
              - light.control:
                  id: step_00
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_01_state) == "on";'
            then:
              - light.control:
                  id: step_01
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_02_state) == "on";'
            then:
              - light.control:
                  id: step_02
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_03_state) == "on";'
            then:
              - light.control:
                  id: step_03
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_04_state) == "on";'
            then:
              - light.control:
                  id: step_04
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_05_state) == "on";'
            then:
              - light.control:
                  id: step_05
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_06_state) == "on";'
            then:
              - light.control:
                  id: step_06
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_07_state) == "on";'
            then:
              - light.control:
                  id: step_07
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_08_state) == "on";'
            then:
              - light.control:
                  id: step_08
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_09_state) == "on";'
            then:
              - light.control:
                  id: step_09
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_10_state) == "on";'
            then:
              - light.control:
                  id: step_10
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_11_state) == "on";'
            then:
              - light.control:
                  id: step_11
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_12_state) == "on";'
            then:
              - light.control:
                  id: step_12
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_13_state) == "on";'
            then:
              - light.control:
                  id: step_13
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_14_state) == "on";'
            then:
              - light.control:
                  id: step_14
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_15_state) == "on";'
            then:
              - light.control:
                  id: step_15
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
        - if:
            condition:
              - lambda: 'return id(light_step_16_state) == "on";'
            then:
              - light.control:
                  id: step_16
                  red: !lambda "return id(color_red);"
                  green: !lambda "return id(color_green);"
                  blue: !lambda "return id(color_blue);"
